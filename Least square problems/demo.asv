
n=500;
%%% generate the matrix C
rand('state',0);
C=rand(n,n); C=(C'+C)-ones(n,n)+eye(n);
%%% generate HL and HU
HU=ones(n,n)*0.2;
HL=-HU;
for i=1:n
    HU(i,i)=1;
    HL(i,i)=1;
end

cor_ADMM(n,C,HL,HU);   % the extended ADMM
cor_pd_pc(n,C)

